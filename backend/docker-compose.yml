services:
  db:
    image: postgres:16-alpine
    container_name: erp-crm-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: erp
      POSTGRES_PASSWORD: erp_password
      POSTGRES_DB: erp_crm
    ports:
      - "5433:5432"
    volumes:
      - erp_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U erp -d erp_crm"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  erp_pgdata:
